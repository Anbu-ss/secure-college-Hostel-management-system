{
  "cleanUrls": true,
  "buildCommand": "npm install --prefix frontend && npm run build --prefix frontend -- --outDir ../dist --emptyOutDir",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://secure-college-hostel-management-system-3.onrender.com/api/:path*"
    },
    {
      "source": "/((?!api|assets|.*\\.).*)",
      "destination": "/index.html"
    }
  ]
}